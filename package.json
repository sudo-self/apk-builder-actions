{
  "name": "apk-builder-actions",
  "version": "1.0.0",
  "description": "GitHub Actions APK Builder",
  "scripts": {
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "boto3": ">=1.26.0",
    "requests": ">=2.28.0"
  },
  "engines": {
    "python": ">=3.8"
  }
}
